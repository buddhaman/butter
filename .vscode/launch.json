{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run go tests",
            "type": "go",
            "request": "launch",
            "mode": "test",
            "program": "${workspaceFolder}/cmd",
            "cwd": "${workspaceFolder}",
        }
    ]
}